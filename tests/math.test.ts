import { describe, it, expect } from 'vitest';

describe('math utilities', () => {
  it('adds two numbers', () => {
    expect(1 + 1).toBe(2);
  });

  it('multiplies two numbers', () => {
    expect(3 * 4).toBe(12);
  });
});
